/*
	 MiG Alley
	 Copyright (C) 1998, 1999, 2000, 2001 Empire Interactive (Europe) Ltd,
	 677 High Road, North Finchley, London N12 0DA

	 Please see the document licence.doc for the full licence agreement

2. LICENCE
 2.1 	
 	Subject to the provisions of this Agreement we now grant to you the 
 	following rights in respect of the Source Code:
  2.1.1 
  	the non-exclusive right to Exploit  the Source Code and Executable 
  	Code on any medium; and 
  2.1.2 
  	the non-exclusive right to create and distribute Derivative Works.
 2.2 	
 	Subject to the provisions of this Agreement we now grant you the
	following rights in respect of the Object Code:
  2.2.1 
	the non-exclusive right to Exploit the Object Code on the same
	terms and conditions set out in clause 3, provided that any
	distribution is done so on the terms of this Agreement and is
	accompanied by the Source Code and Executable Code (as
	applicable).

3. GENERAL OBLIGATIONS
 3.1 
 	In consideration of the licence granted in clause 2.1 you now agree:
  3.1.1 
	that when you distribute the Source Code or Executable Code or
	any Derivative Works to Recipients you will also include the
	terms of this Agreement;
  3.1.2 
	that when you make the Source Code, Executable Code or any
	Derivative Works ("Materials") available to download, you will
	ensure that Recipients must accept the terms of this Agreement
	before being allowed to download such Materials;
  3.1.3 
	that by Exploiting the Source Code or Executable Code you may
	not impose any further restrictions on a Recipient's subsequent
	Exploitation of the Source Code or Executable Code other than
	those contained in the terms and conditions of this Agreement;
  3.1.4 
	not (and not to allow any third party) to profit or make any
	charge for the Source Code, or Executable Code, any
	Exploitation of the Source Code or Executable Code, or for any
	Derivative Works;
  3.1.5 
	not to place any restrictions on the operability of the Source 
	Code;
  3.1.6 
	to attach prominent notices to any Derivative Works stating
	that you have changed the Source Code or Executable Code and to
	include the details anddate of such change; and
  3.1.7 
  	not to Exploit the Source Code or Executable Code otherwise than
	as expressly permitted by  this Agreement.

questions about this file may be asked at http://www.simhq.com/
*/

// Machine generated IDispatch wrapper class(es) created by Microsoft Visual C++

// NOTE: Do not modify the contents of this file.  If this class is regenerated by
//  Microsoft Visual C++, your modifications will be overwritten.

#include <iostream>
#include <map>
#include "WIN32_COMPAT.H" //RERUN
#include "MFC_stub.h"
#include "DLGITEM.H"
#include "RDIALOG.H"
#include "RCOMBO.H"

#include "RESOURCE.H" //RERUN
#include "MIGVIEW.H" //RERUN for GetFullPanel

struct RComboData {
    int fontNum = 2; // Default font
};

static std::map<CWnd*, RComboData> g_rcombo_data;

static RComboData& GetData(CWnd* wnd) {
    // operator[] will create a default-constructed RComboData if wnd is not found.
    // This is safe because the default fontNum is a sensible value.
    return g_rcombo_data[wnd];
}

/////////////////////////////////////////////////////////////////////////////
// CRCombo

CRCombo::CRCombo() : m_curSel(0), m_dropDown(nullptr), m_listboxLength(0) {
    // Initialize any necessary members here
}

CRCombo::~CRCombo() {
    // Clean up any allocated resources here
    if (m_dropDown) {
        m_dropDown->DestroyWindow();
        delete m_dropDown;
        m_dropDown = nullptr;
    }
}

IMPLEMENT_DYNCREATE(CRCombo, CWnd)


/////////////////////////////////////////////////////////////////////////////
// CRCombo properties

OLE_COLOR CRCombo::GetForeColor()
{
	OLE_COLOR result;
	GetProperty(DISPID_FORECOLOR, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetForeColor(OLE_COLOR propVal)
{
	SetProperty(DISPID_FORECOLOR, VT_I4, propVal);
}

CString CRCombo::GetCaption()
{
	CString result;
	GetProperty(DISPID_CAPTION, VT_BSTR, (void*)&result);
	return result;
}

void CRCombo::SetCaption(LPCTSTR propVal)
{
	SetProperty(DISPID_CAPTION, VT_BSTR, propVal);
}

long CRCombo::GetFontNum()
{
	return GetData(this).fontNum;
}

void CRCombo::SetFontNum(long propVal)
{
	GetData(this).fontNum = propVal;
}

long CRCombo::GetListboxLength()
{
	return m_listboxLength;
}

void CRCombo::SetListboxLength(long propVal)
{
    m_listboxLength = propVal;
    Invalidate(); // Trigger a redraw if needed
}


BOOL CRCombo::GetCircularStyle()
{
	BOOL result;
	GetProperty(0x3, VT_BOOL, (void*)&result);
	return result;
}

void CRCombo::SetCircularStyle(BOOL propVal)
{
	SetProperty(0x3, VT_BOOL, propVal);
}

BOOL CRCombo::GetEnabled()
{
	BOOL result;
	GetProperty(DISPID_ENABLED, VT_BOOL, (void*)&result);
	return result;
}

void CRCombo::SetEnabled(BOOL propVal)
{
	SetProperty(DISPID_ENABLED, VT_BOOL, propVal);
}

unsigned long CRCombo::GetShadowColor()
{
	unsigned long result;
	GetProperty(0x4, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetShadowColor(unsigned long propVal)
{
	SetProperty(0x4, VT_I4, propVal);
}

long CRCombo::GetEndFileNum()
{
	long result;
	GetProperty(0x5, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetEndFileNum(long propVal)
{
	SetProperty(0x5, VT_I4, propVal);
}

long CRCombo::GetFileNumMain()
{
	long result;
	GetProperty(0x6, VT_I4, (void*)&result);
	return result;
}

void CRCombo::SetFileNumMain(long propVal)
{
	SetProperty(0x6, VT_I4, propVal);
}

/////////////////////////////////////////////////////////////////////////////
// CRCombo operations

void CRCombo::AddString(LPCTSTR text)
{
	// Add the string to the internal list
    m_items.push_back(text);
}

long CRCombo::GetListbox()
{
	long result;
	InvokeHelper(0x8, DISPATCH_METHOD, VT_I4, (void*)&result, NULL);
	return result;
}

void CRCombo::SetIndex(long row)
{
	//static BYTE parms[] =
	//	VTS_I4;
	//InvokeHelper(0x9, DISPATCH_METHOD, VT_EMPTY, NULL, parms,
	//	 row);
	SetCurSel(row);
}

long CRCombo::GetIndex()
{
	//long result;
	//InvokeHelper(0xa, DISPATCH_METHOD, VT_I4, (void*)&result, NULL);
	//return result;
	return GetCurSel();
}

void CRCombo::Clear()
{
    m_items.clear();
    m_curSel = -1;
    SetWindowText("");
    Invalidate();
}

void CRCombo::DeleteString(long index)
{
    if (index >= 0 && index < (int)m_items.size())
    {
        m_items.erase(m_items.begin() + index);
        if (m_curSel == index)
            m_curSel = -1;
        else if (m_curSel > index)
            m_curSel--;
        Invalidate();
    }
}

int CRCombo::GetCurSel() const 
{ 
	return m_curSel; 
}

int CRCombo::SetCurSel(int index) {
	if (index >= -1 && index < (int)m_items.size()) {
		if (m_curSel != index) {
			m_curSel = index;
			if (m_curSel >= 0) {
				SetWindowText(m_items[m_curSel].c_str());
			} else {
				SetWindowText("");
			}
		} else {
			// Even if index is the same, we might need to repaint if the dropdown was just closed.
			Invalidate();
		}
		return m_curSel;
	}
	return CB_ERR;
}

void CRCombo::ResetContent() {
    m_items.clear();
    m_curSel = -1;
    SetWindowText("");
    Invalidate();
}

void CRCombo::OnPaint()
{
    CPaintDC dc(this);
    CRect rect;
    GetClientRect(rect);

    // Simple "Rowan Combo" look: Yellow text on dark background, or standard gray
    //dc.FillSolidRect(rect, RGB(64, 64, 64));
    dc.Draw3dRect(rect, RGB(255, 255, 255), RGB(0, 0, 0));

    CString text;
    GetWindowText(text);
    if (text.IsEmpty() && m_curSel >= 0 && m_curSel < (int)m_items.size()) {
		text = m_items[m_curSel].c_str();
	}

    CFont* pFont = (CFont*)GetParent()->SendMessage(WM_GETGLOBALFONT, GetFontNum());
    if (pFont) dc.SelectObject(pFont);

    dc.SetTextColor(RGB(255, 255, 0)); // Yellow text
    dc.SetBkMode(TRANSPARENT);
    
    CRect textRect(rect.left + 2, rect.top + 2, rect.right - 20, rect.bottom - 2);
    dc.DrawText(text, textRect, DT_LEFT | DT_VCENTER | DT_SINGLELINE);

    // Draw a small arrow on the right
    dc.FillSolidRect(rect.right - 16, rect.top + 2, 14, rect.Height() - 4, RGB(128, 128, 128));
    dc.Draw3dRect(rect.right - 16, rect.top + 2, 14, rect.Height() - 4, RGB(255, 255, 255), RGB(0, 0, 0));
}

void CRCombo::OnLButtonDown(UINT nFlags, CPoint point) {
    // Implement dropdown behavior here
    // This is a simplified version - a real dropdown would require a popup window
    //  to display the options.
    // CWnd::OnLButtonDown(nFlags, point); // RERUN: Do not call base, it captures the mouse
    if (GetParent()) {
        // RERUN: If dropdown is already visible, just hide it and do nothing else.
        if (m_dropDown && m_dropDown->IsWindowVisible()) {
            m_dropDown->ShowWindow(SW_HIDE);
            return;
        }

        // RERUN: Send a CBN_DROPDOWN notification so the parent knows the list is about to appear.
        GetParent()->SendMessage(WM_COMMAND, MAKEWPARAM(GetDlgCtrlID(), CBN_DROPDOWN), (LPARAM)m_hWnd);
    }

    // Create the CRListBox directly in CRCombo
    if (!m_dropDown) {
        CRect rect(0, 0, 100, 100);
        m_dropDown = new CListBox();
        // RERUN: Create as a top-level popup window (NULL parent) so it can draw over everything.
        m_dropDown->Create("LISTBOX", NULL, WS_POPUP | WS_VISIBLE | LBS_NOTIFY, rect, NULL, GetDlgCtrlID() + 1000);
        m_dropDown->m_pComboOwner = this; // RERUN: Set the owner
    }
    
    // Populate the list box
    m_dropDown->ResetContent();
    for (size_t i = 0; i < m_items.size(); ++i) {
        m_dropDown->AddString(m_items[i].c_str());
    }
    m_dropDown->SetCurSel(m_curSel);

    CRect comboRect;
    GetWindowRect(&comboRect); // Gets screen coordinates

    int itemCount = m_items.size();
    int dropWidth = comboRect.Width();
    // RERUN: Calculate itemHeight directly here to ensure it's consistent with the context
    // in which the dropdown is being created, avoiding potential mismatches with later
    // calculations during OnPaint or OnLButtonDown.
    
    CDC* pDC = GetDC();
    CFont* pFont = (CFont*)GetParent()->SendMessage(WM_GETGLOBALFONT, GetFontNum());
    
    // Pass the correct font to the dropdown so its internal calculations match ours
    m_dropDown->OnSetFont(pFont);
    int itemHeight = m_dropDown->GetItemHeight(); // RERUN: Use the listbox's cached height

    if (pDC) {
        // Get the correct font for the listbox, using the combo's font number
        // and its parent's (the dialog's) message handler.

        for (const auto& item : m_items) {
            CSize sz = pDC->GetTextExtent(item.c_str());
            if (sz.cx + 10 > dropWidth) { dropWidth = sz.cx + 10; }
        }
        ReleaseDC(pDC);
    }
    int dropHeight = std::min(itemCount, 25) * itemHeight + 4; // Show max 25 items + border

    m_dropDown->MoveWindow(comboRect.left, comboRect.bottom, dropWidth, dropHeight);
    //Show the pop up window
    m_dropDown->ShowWindow(SW_SHOW);
    // RERUN: Capture the mouse so that all mouse events go to the dropdown list,
    // ensuring correct coordinate calculation and allowing clicks outside to be caught.
    m_dropDown->SetCapture();
    m_dropDown->BringWindowToTop(); // RERUN: Ensure dropdown is drawn on top of other dialogs.
}

void CRCombo::OnLButtonUp(UINT nFlags, CPoint point) {
    // RERUN: This method is now empty. We are not capturing the mouse on the combo,
    // so there is no need to release it here.
}