/*
	 MiG Alley
	 Copyright (C) 1998, 1999, 2000, 2001 Empire Interactive (Europe) Ltd,
	 677 High Road, North Finchley, London N12 0DA

	 Please see the document licence.doc for the full licence agreement

2. LICENCE
 2.1 	
 	Subject to the provisions of this Agreement we now grant to you the 
 	following rights in respect of the Source Code:
  2.1.1 
  	the non-exclusive right to Exploit  the Source Code and Executable 
  	Code on any medium; and 
  2.1.2 
  	the non-exclusive right to create and distribute Derivative Works.
 2.2 	
 	Subject to the provisions of this Agreement we now grant you the
	following rights in respect of the Object Code:
  2.2.1 
	the non-exclusive right to Exploit the Object Code on the same
	terms and conditions set out in clause 3, provided that any
	distribution is done so on the terms of this Agreement and is
	accompanied by the Source Code and Executable Code (as
	applicable).

3. GENERAL OBLIGATIONS
 3.1 
 	In consideration of the licence granted in clause 2.1 you now agree:
  3.1.1 
	that when you distribute the Source Code or Executable Code or
	any Derivative Works to Recipients you will also include the
	terms of this Agreement;
  3.1.2 
	that when you make the Source Code, Executable Code or any
	Derivative Works ("Materials") available to download, you will
	ensure that Recipients must accept the terms of this Agreement
	before being allowed to download such Materials;
  3.1.3 
	that by Exploiting the Source Code or Executable Code you may
	not impose any further restrictions on a Recipient's subsequent
	Exploitation of the Source Code or Executable Code other than
	those contained in the terms and conditions of this Agreement;
  3.1.4 
	not (and not to allow any third party) to profit or make any
	charge for the Source Code, or Executable Code, any
	Exploitation of the Source Code or Executable Code, or for any
	Derivative Works;
  3.1.5 
	not to place any restrictions on the operability of the Source 
	Code;
  3.1.6 
	to attach prominent notices to any Derivative Works stating
	that you have changed the Source Code or Executable Code and to
	include the details anddate of such change; and
  3.1.7 
  	not to Exploit the Source Code or Executable Code otherwise than
	as expressly permitted by  this Agreement.

questions about this file may be asked at http://www.simhq.com/
*/

// Machine generated IDispatch wrapper class(es) created by Microsoft Visual C++

// NOTE: Do not modify the contents of this file.  If this class is regenerated by
//  Microsoft Visual C++, your modifications will be overwritten.

#include <iostream>
#include "WIN32_COMPAT.H"
#include "MFC_stub.h"
#include "DLGITEM.H"
#include "RDIALOG.H"
#include "MIG.H"
#include "MIGVIEW.H"

#include "RSTATIC.H"
#include <map> // RERUN

// RERUN: Static map to store picture IDs since CWnd::SetProperty is a stub
static std::map<CWnd*, long> g_static_pics;

BEGIN_MESSAGE_MAP(CRStatic, CWnd)
    ON_WM_PAINT()
END_MESSAGE_MAP()

/////////////////////////////////////////////////////////////////////////////
// CRStatic

IMPLEMENT_DYNCREATE(CRStatic, CWnd)

/////////////////////////////////////////////////////////////////////////////
// CRStatic properties

CString CRStatic::GetCaption()
{
	CString str;
	GetWindowText(str);
	return str;
}

void CRStatic::SetCaption(LPCTSTR propVal)
{
	SetWindowText(propVal);
}

OLE_COLOR CRStatic::GetForeColor()
{
	OLE_COLOR result;
	GetProperty(DISPID_FORECOLOR, VT_I4, (void*)&result);
	return result;
}

void CRStatic::SetForeColor(OLE_COLOR propVal)
{
	SetProperty(DISPID_FORECOLOR, VT_I4, propVal);
}

BOOL CRStatic::GetUpdateCaption()
{
	BOOL result;
	GetProperty(0x1, VT_BOOL, (void*)&result);
	return result;
}

void CRStatic::SetUpdateCaption(BOOL propVal)
{
	SetProperty(0x1, VT_BOOL, propVal);
}

long CRStatic::GetFontNum()
{
	return m_fontNum;
}

void CRStatic::SetFontNum(long propVal)
{
	m_fontNum = propVal;
}

CString CRStatic::GetString()
{
	CString str;
	GetWindowText(str);
	return str;
}

void CRStatic::SetString(LPCTSTR propVal)
{
	SetWindowText(propVal);
}

long CRStatic::GetResourceNumber()
{
	long result;
	GetProperty(0x4, VT_I4, (void*)&result);
	return result;
}

void CRStatic::SetResourceNumber(long propVal)
{
	m_resourceNum = propVal;
}

long CRStatic::GetPictureFileNum()
{
	long result;
	GetProperty(0x5, VT_I4, (void*)&result);
	return result;
}

void CRStatic::SetPictureFileNum(long propVal)
{
	g_static_pics[this] = propVal; // RERUN: Store locally
	SetProperty(0x5, VT_I4, propVal);
	Invalidate();
}

BOOL CRStatic::GetCentral()
{
	BOOL result;
	GetProperty(0x6, VT_BOOL, (void*)&result);
	return result;
}

void CRStatic::SetCentral(BOOL propVal)
{
	SetProperty(0x6, VT_BOOL, propVal);
}

unsigned long CRStatic::GetShadowColor()
{
	unsigned long result;
	GetProperty(0x7, VT_I4, (void*)&result);
	return result;
}

void CRStatic::SetShadowColor(unsigned long propVal)
{
	SetProperty(0x7, VT_I4, propVal);
}

/////////////////////////////////////////////////////////////////////////////
// CRStatic operations

void CRStatic::OnPaint()
{
	// RERUN: Custom OnPaint to handle text drawing, similar to CStatic
    CPaintDC dc(this);
    CRect rect;
    GetClientRect(&rect);

	// RERUN: Draw Icon if present
    if (g_static_pics.count(this)) {
        long artNum = g_static_pics[this];
        if (artNum != 0) { 
             fileblock picture((FileNum)artNum);
             BYTE* pData = (BYTE*)getdata(picture);
             if (pData && pData[0] == 'B' && pData[1] == 'M') {
                 BITMAPFILEHEADER* pFile = (BITMAPFILEHEADER*)pData;
                 BITMAPINFO* pInfo = (BITMAPINFO*)(pData + sizeof(BITMAPFILEHEADER));
                 pData += pFile->bfOffBits;
                 int w = pInfo->bmiHeader.biWidth;
                 int h = pInfo->bmiHeader.biHeight;
                 
                 // RERUN FIX: Draw 1:1 using original dimensions to preserve aspect ratio.
                 // Do not stretch to rect.Width() because that represents capacity, not image size.
                 int drawW = w;
                 int drawH = abs(h);
                 
                 // Center vertically
                 int y = (rect.Height() - drawH) / 2;
                 
                 StretchDIBits(dc.GetSafeHdc(), 0, y, drawW, drawH, 0, 0, w, h, pData, pInfo, DIB_RGB_COLORS, SRCCOPY);
             }
        }
    }
    
    CString text;
    GetWindowText(text);
    
    // Ensure we have a font
    CFont* pFont = m_pFont; // Check if SetFont was called on CWnd
    if (!pFont && GetParent()) {
        // Try to get font from parent using our font property index
        // 0=Small, 1=Title, 2=Normal, 3=Large typically
        pFont = (CFont*)GetParent()->SendMessage(WM_GETGLOBALFONT, m_fontNum);
    }
    
    CFont* pOldFont = nullptr;
    if (pFont) {
        pOldFont = dc.SelectObject(pFont);
    }

    if (!text.IsEmpty())
    {
        dc.SetBkMode(TRANSPARENT);
        dc.SetTextColor(RGB(173, 216, 230)); // RERUN: Force light blue text

        UINT format = DT_WORDBREAK;
        DWORD style = GetStyle();
        if (style & SS_CENTER) format |= DT_CENTER;
        else if (style & SS_RIGHT) format |= DT_RIGHT;
        else format |= DT_LEFT;
        
        format |= DT_VCENTER;
		//std::cout << "[DEBUG] CRStatic::OnPaint: Drawing text '" << text << "' with font num " << m_fontNum << std::endl;
        dc.DrawText(text, rect, format);
    }
    
    if (pOldFont) {
        dc.SelectObject(pOldFont);
    }
}
