/*
	 MiG Alley
	 Copyright (C) 1998, 1999, 2000, 2001 Empire Interactive (Europe) Ltd,
	 677 High Road, North Finchley, London N12 0DA

	 Please see the document licence.doc for the full licence agreement

2. LICENCE
 2.1 	
 	Subject to the provisions of this Agreement we now grant to you the 
 	following rights in respect of the Source Code:
  2.1.1 
  	the non-exclusive right to Exploit  the Source Code and Executable 
  	Code on any medium; and 
  2.1.2 
  	the non-exclusive right to create and distribute Derivative Works.
 2.2 	
 	Subject to the provisions of this Agreement we now grant you the
	following rights in respect of the Object Code:
  2.2.1 
	the non-exclusive right to Exploit the Object Code on the same
	terms and conditions set out in clause 3, provided that any
	distribution is done so on the terms of this Agreement and is
	accompanied by the Source Code and Executable Code (as
	applicable).

3. GENERAL OBLIGATIONS
 3.1 
 	In consideration of the licence granted in clause 2.1 you now agree:
  3.1.1 
	that when you distribute the Source Code or Executable Code or
	any Derivative Works to Recipients you will also include the
	terms of this Agreement;
  3.1.2 
	that when you make the Source Code, Executable Code or any
	Derivative Works ("Materials") available to download, you will
	ensure that Recipients must accept the terms of this Agreement
	before being allowed to download such Materials;
  3.1.3 
	that by Exploiting the Source Code or Executable Code you may
	not impose any further restrictions on a Recipient's subsequent
	Exploitation of the Source Code or Executable Code other than
	those contained in the terms and conditions of this Agreement;
  3.1.4 
	not (and not to allow any third party) to profit or make any
	charge for the Source Code, or Executable Code, any
	Exploitation of the Source Code or Executable Code, or for any
	Derivative Works;
  3.1.5 
	not to place any restrictions on the operability of the Source 
	Code;
  3.1.6 
	to attach prominent notices to any Derivative Works stating
	that you have changed the Source Code or Executable Code and to
	include the details anddate of such change; and
  3.1.7 
  	not to Exploit the Source Code or Executable Code otherwise than
	as expressly permitted by  this Agreement.

questions about this file may be asked at http://www.simhq.com/
*/

// Machine generated IDispatch wrapper class(es) created by Microsoft Visual C++

// NOTE: Do not modify the contents of this file.  If this class is regenerated by
//  Microsoft Visual C++, your modifications will be overwritten.


#include "WIN32_COMPAT.H"
#include "MFC_stub.h"
#include "DLGITEM.H"
#include "RDIALOG.H"
#include "MIG.H"
#include "MIGVIEW.H"
#include "RRADIO.H"
#include "FILES.H"

/////////////////////////////////////////////////////////////////////////////
// CRRadio

IMPLEMENT_DYNCREATE(CRRadio, DlgItem)

BEGIN_MESSAGE_MAP(CRRadio, DlgItem)
    ON_WM_PAINT()
    ON_WM_LBUTTONDOWN()
    ON_WM_LBUTTONUP()
END_MESSAGE_MAP()

CRRadio::CRRadio()
{
    m_fontNum = 9;
    m_cols = 1;
    m_currentSelection = -1;
    m_colWidth = 100;
    m_foreColor = RGB(255, 255, 0); // Default to yellow for visibility
    m_artNum = 0;
}

CRRadio::~CRRadio()
{
}

/////////////////////////////////////////////////////////////////////////////
// CRRadio properties

long CRRadio::GetFontNum()
{
    return m_fontNum;
}

void CRRadio::SetFontNum(long propVal)
{
    m_fontNum = propVal;
    Invalidate();
}

long CRRadio::GetCols()
{
    return m_cols;
}

void CRRadio::SetCols(long propVal)
{
    if (propVal > 0) {
        m_cols = propVal;
        Invalidate();
    }
}

long CRRadio::GetCurrentSelection()
{
    return m_currentSelection;
}

void CRRadio::SetCurrentSelection(long propVal)
{
    if (propVal >= -1 && propVal < (long)m_buttons.size()) {
        if (m_currentSelection != propVal) {
            m_currentSelection = propVal;
            Invalidate();
        }
    }
}

long CRRadio::GetColumnWidth()
{
    return m_colWidth;
}

void CRRadio::SetColumnWidth(long propVal)
{
    if (propVal > 0) {
        m_colWidth = propVal;
        Invalidate();
    }
}

OLE_COLOR CRRadio::GetForeColor()
{
    return m_foreColor;
}

void CRRadio::SetForeColor(OLE_COLOR propVal)
{
    m_foreColor = propVal;
    Invalidate();
}

/////////////////////////////////////////////////////////////////////////////
// CRRadio operations

void CRRadio::SetFileNum(long val)
{
    m_artNum = val;
    Invalidate();
}

void CRRadio::AddButton(LPCTSTR text)
{
    m_buttons.push_back(text);
    Invalidate();
}

void CRRadio::Clear()
{
    m_buttons.clear();
    m_currentSelection = -1;
    Invalidate();
}

void CRRadio::OnPaint()
{
    CPaintDC dc(this);
    CRect rect;
    GetClientRect(rect);

    CFont* pFont = nullptr; // Initialize pFont to nullptr

    // Send WM_GETGLOBALFONT to the parent window, allowing it to handle font retrieval
    // or propagate the message up the hierarchy until an RDialog or RFullPanelDial handles it.
    if (GetParent()) {
        pFont = (CFont*)GetParent()->SendMessage(WM_GETGLOBALFONT, m_fontNum);
    }
    CFont* pOldFont = nullptr;
    if (pFont) {
        pOldFont = dc.SelectObject(pFont);
    }

    dc.SetTextColor(m_foreColor);
    dc.SetBkMode(TRANSPARENT);

    if (m_buttons.empty()) {
        if (pOldFont) dc.SelectObject(pOldFont);
        return;
    }

    CSize textSize = dc.GetTextExtent("Wg");
    int buttonHeight = textSize.cy + 4;
    int buttonRadius = buttonHeight / 4; // Default for circular radio buttons
    int textOffset = buttonRadius * 2 + 4; // Initial offset for circles

    // RERUN: If cols is 1 but rect is wider than tall, assume horizontal intent
    int cols = m_cols;
    if (cols == 1 && rect.Width() > rect.Height() * 2 && m_buttons.size() > 1)
        cols = m_buttons.size();

    int colWidth = (m_colWidth > 0) ? m_colWidth : rect.Width() / cols;

    for (size_t i = 0; i < m_buttons.size(); ++i) {
        int row = i / cols;
        int col = i % cols;

        int x = col * colWidth;
        int y = row * buttonHeight;

        int circleY = y + buttonHeight / 2;

        if (m_artNum)
        {
            long currentartnum = m_artNum;
            if ((long)i == m_currentSelection)
            {
                currentartnum++;
            }

            // Draw artwork
            fileblock art((FileNum)currentartnum);
            BYTE* pData = (BYTE*)getdata(art);
            if (pData && pData[0] == 'B' && pData[1] == 'M')
            {
                BITMAPFILEHEADER* pFile = (BITMAPFILEHEADER*)pData;
                BITMAPINFO* pInfo = (BITMAPINFO*)(pData + sizeof(BITMAPFILEHEADER));
                BYTE* pBits = pData + pFile->bfOffBits;

                int imgW = pInfo->bmiHeader.biWidth;
                int imgH = abs(pInfo->bmiHeader.biHeight);
                
                // Assume horizontal strip if width > height
                int frames = (imgW > imgH) ? (imgW / imgH) : 1;
                int frameW = imgW / frames;
                int frameH = imgH;
                
                int state = ((long)i == m_currentSelection) ? 1 : 0;
                if (state >= frames) state = 0;
                
                StretchDIBits(dc.m_hDC, x, circleY - frameH/2, frameW, frameH, 
                              state * frameW, 0, frameW, frameH, 
                              pBits, pInfo, DIB_RGB_COLORS, SRCCOPY);
                textOffset = frameW + 4; // Adjust textOffset based on bitmap width + padding
            }
        }
        else
        {
            // Draw radio button circle
            CPen pen(PS_SOLID, 1, m_foreColor);
            CPen* pOldPen = dc.SelectObject(&pen);
            CBrush* pOldBrush = (CBrush*)dc.SelectStockObject(NULL_BRUSH);
            dc.Ellipse(x, circleY - buttonRadius, x + buttonRadius * 2, circleY + buttonRadius);
            dc.SelectObject(pOldPen);
            dc.SelectObject(pOldBrush);
            // Draw selection dot
            if ((long)i == m_currentSelection) 
            {
                CBrush brush(m_foreColor);
                CBrush* oldBrush = dc.SelectObject(&brush);
                dc.Ellipse(x + 3, circleY - buttonRadius + 3, x + buttonRadius * 2 - 3, circleY + buttonRadius - 3);
                dc.SelectObject(oldBrush);
            }
            textOffset = buttonRadius * 2 + 4; // Keep original offset for circles
        }

        // Draw text
        dc.TextOut(x + textOffset, y + 2, m_buttons[i]);
    }

    if (pOldFont) {
        dc.SelectObject(pOldFont);
    }
}

void CRRadio::OnLButtonDown(UINT nFlags, CPoint point)
{
    if (m_buttons.empty()) return;

    CFont* pFont = (CFont*)SendMessage(WM_GETGLOBALFONT, m_fontNum);
    CDC dc(this);
    CFont* pOldFont = nullptr;
    if (pFont) {
        pOldFont = dc.SelectObject(pFont);
    }
    CSize textSize = dc.GetTextExtent("Wg");
    if (pOldFont) {
        dc.SelectObject(pOldFont);
    }

    int buttonHeight = textSize.cy + 4;
    if (buttonHeight <= 0) return;

    CRect rect;
    GetClientRect(rect);
    int cols = m_cols;
    if (cols == 1 && rect.Width() > rect.Height() * 2 && m_buttons.size() > 1)
        cols = m_buttons.size();

    int colWidth = (m_colWidth > 0) ? m_colWidth : rect.Width() / cols;
    if (colWidth <= 0) return;

    int row = point.y / buttonHeight;
    int col = point.x / colWidth;
    int index = row * cols + col;
    int totalRows = (m_buttons.size() + cols - 1) / cols;

    // Only process the click if it actually hits a button in the grid
    if (index >= 0 && index < (int)m_buttons.size() && row < totalRows) 
    {
        SetCurrentSelection(index);
        // RERUN: Fire event only on user interaction to avoid init loops
        FireEvent(1, m_currentSelection);
        // Take capture so we get the Up message
        DlgItem::OnLButtonDown(nFlags, point);
    }
}

void CRRadio::OnLButtonUp(UINT nFlags, CPoint point)
{
    // Release mouse capture so other controls (like drop-downs) can receive input.
    DlgItem::OnLButtonUp(nFlags, point);
}
